<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
    <title>Productos</title>
</head>

<body>
    <div class="container d-flex p-5">
        <div class="jumbotron w-75 m-auto d-flex flex-column">
            <h1 class="text-center">Productos</h1>
            <a href="http://localhost:8080" class="align-self-center m-2">ir formulario de carga</a>
            <div class="d-flex flex-column text-center justify-content-center border-top border-dark py-4">
                <% if(items.length) { %>
                <% items.forEach(product => {%>
                <h4>Nombre: <%= product.title %></h4>
                <img src="<%= product.thumbnail %>" alt="" width="100" class="align-self-center">
                <p>Precio: $<%= product.price %></p>
                <% }) %>
                <% } else { %>
                <h1>No hay productos</h1>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>